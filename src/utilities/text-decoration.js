import { getColor, style } from '@xstyled/styled-components'

export const textDecorationThickness = style({
  prop: 'textDecorationThickness',
})

export const textUnderlineOffset = style({
  prop: 'textUnderlineOffset',
})
